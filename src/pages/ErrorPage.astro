---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Acceso Denegado">
    <link
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    rel="stylesheet"
  />
  <main class="flex min-h-screen bg-gray-100 justify-center items-center">
    <div class="text-center bg-white p-10 rounded-lg shadow-lg max-w-md">
        <i class="fa-solid fa-hand text-red-600 text-3xl"></i>
      <h1 class="text-3xl font-bold text-red-600 mb-2">¡Alto ahí!</h1>
      <p class="text-gray-700 mb-6">
        Parece que intentaste entrar a un área restringida.<br />
        No te preocupes, a todos nos pasa... una vez.
      </p>
      <p class="text-gray-500 italic mb-4">
        (Quizás deberías ser recepcionista para acceder 😜)
      </p>
      <button
        onclick="redirectToLogin()"
        class="bg-blue-500 text-white py-2 px-6 rounded-md shadow hover:bg-blue-600 focus:ring-2 focus:ring-blue-400"
      >
        Llévame de vuelta
      </button>
      <p class="mt-4 text-sm text-gray-500">
        Redirigiendo automáticamente en <span id="countdown">5</span> segundos...
      </p>
    </div>
  </main>

  <script>
    let countdown = 5;
    const countdownElement = document.getElementById('countdown');

    function redirectToLogin() {
      window.location.href = '/'; // Replace with your login or home page
    }

    const interval = setInterval(() => {
      countdown -= 1;
      countdownElement.textContent = countdown;

      if (countdown <= 0) {
        clearInterval(interval);
        redirectToLogin();
      }
    }, 1000);
  </script>
</Layout>
